there are there stages of compilation
.exe --> executable file or binary file. it has 0s nad 1s

Compilation is the process of changing an ASCII codefile of any language into machine code

preprocessing --> compilation --> linking  


# --> preprocessor directive

#include 
#define unsigned long long int ull
#pragma

ashu main() {

    for(ull i = 0; i < n;)   2^32
    int a, b;
    ashu a, b;
}



struct  --> user defined data type / non primitive data type 
//int, char, bool, float, double

//1 byte --> 0 - 255
/*
    int --> 4 byte --> 32 bits 
    float --> 4 byte -- > 32 bits       //upto six digits of precision 
    char --> 1 byte --> 8 bits
    bool -> 4 byte (not sure)
    double --> 8 bytes            //20 digits of precision

*/

int class[100]    //array is a collection of entities having similar data type
// student array[10]

struct {
    int age;
    char name[10];

}

//collection of dissimilar data memebers and functions to represent a real world entity