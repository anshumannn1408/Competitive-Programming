#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t , a , b;
    cin >> t;
    while(t--)
    {
        cin >> a >> b;
        a += b;
        cout << a << endl;
    }
    return 0;
}
